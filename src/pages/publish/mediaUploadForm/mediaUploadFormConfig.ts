/**
 * mediaUploadFormConfig.ts
 *
 * Konstanten für MediaUploadForm.tsx — 1:1 aus MediaUploadForm.tsx
 * verschoben (PLAN3.md Schritt 1). Reines Verschieben, keine
 * Logik-Änderungen.
 */

// Aus dem Edit-Daten- useEffect (ehem. Z. 240)
export const natureSubcategories = ['tiere', 'blumen', 'strand', 'berge', 'wald', 'meer'];

// Aus dem Edit-Daten- useEffect (ehem. Z. 266)
export const countryTags = ['portugal', 'spanien', 'frankreich', 'belgien', 'deutschland', 'luxemburg'];

// Aus handleSubmit (ehem. Z. 762)
export const countryList = ['portugal', 'spanien', 'frankreich', 'belgien', 'deutschland', 'luxemburg'];

// Aus handleSubmit (ehem. Z. 778)
export const mojobusTag = 'mojobus';
